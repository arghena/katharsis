# NOTE: Dependabot won't follow `labels: []`.
# https://github.com/dependabot/dependabot-core/issues/11783

version: 2

# TODO: Allow lower case commit message.
# https://github.com/dependabot/dependabot-core/issues/10586
updates:
  # FIX: Dependabot doesn't update `Cargo.toml`.
  # https://github.com/dependabot/dependabot-core/issues/14196
  - package-ecosystem: 'cargo'
    directory: '/'
    schedule:
      interval: 'cron'
      # Runs every Monday.
      cronjob: '0 0 * * 1'
    cooldown:
      default-days: 7
    open-pull-requests-limit: 10
    labels: ['build']
    commit-message:
      prefix: 'build'
      include: 'scope'
  - package-ecosystem: 'github-actions'
    directory: '/'
    schedule:
      interval: 'cron'
      # Runs every Monday.
      cronjob: '0 0 * * 1'
    cooldown:
      default-days: 7
    open-pull-requests-limit: 10
    labels: ['ci']
    commit-message:
      prefix: 'ci'
      include: 'scope'
    groups:
      # NOTE: Dependabot now splits path-based actions if they use commit SHAs.
      # https://github.com/dependabot/dependabot-core/pull/14806
      codeql:
        patterns: ['github/codeql-action/*']
